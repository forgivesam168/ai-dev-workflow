name: verify-sync

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  sync_check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Sync check (.github mirror)
        shell: pwsh
        run: pwsh -File ./tools/check-sync.ps1
